<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

<header id="header" class="header d-flex align-items-center fixed-top">
    <div class="header-container container-fluid container-xl position-relative d-flex align-items-center justify-content-between">

        <a href="/" class="logo d-flex align-items-center me-auto me-xl-0">
            <img src="assets/img/logo.webp" alt="">
            <h1 class="sitename">PukHub</h1>
        </a>

        <nav id="navmenu" class="navmenu">
            <ul>
                <li><a href="/#hero" class="active">Acasă</a></li>
                <li><a href="/#about">Despre</a></li>
                <li><a href="/#services">Servicii</a></li>
                <li><a href="/#portfolio">Portofoliu</a></li>
                <li><a href="/chestionar">Chestionar</a></li>
                <li><a href="/#team">Echipă</a></li>

                <li class="dropdown">
                    <a href="#"><span>Coș</span> <i class="bi bi-chevron-down toggle-dropdown"></i></a>
                    <ul>
                        <li><a href="/cos"><i class="bi bi-bag me-1"></i> Vezi coșul</a></li>
                        <% if (typeof cart !== 'undefined' && cart.length > 0) { %>
                            <li><hr class="dropdown-divider"></li>
                            <% cart.slice(0, 3).forEach(p => { %>
                                <li class="small px-3">
                                    🛒 <%= p.nume %> - <%= p.pret %>€
                                </li>
                            <% }) %>
                            <% if (cart.length > 3) { %>
                                <li><a href="/cos" class="text-center d-block small">...mai multe</a></li>
                            <% } %>
                        <% } else { %>
                            <li><span class="px-3 small text-muted">Coșul este gol</span></li>
                        <% } %>
                    </ul>
                </li>

                <li><a href="/#contact">Contact</a></li>
            </ul>
            <i class="mobile-nav-toggle d-xl-none bi bi-list"></i>
        </nav>

        <% if (typeof utilizator !== 'undefined') { %>
            <div class="d-flex align-items-center gap-3 align-self-center">
                <span class="text-light small d-none d-md-block">
                    <i class="bi bi-person-circle me-1"></i>
                    Autentificat ca <strong><%= utilizator.nume %> <%= utilizator.prenume %></strong>
                </span>
                <a class="btn-getstarted" href="/logout">
                    <i class="bi bi-box-arrow-right me-1"></i> Delogare
                </a>
            </div>
        <% } else { %>
            <a class="btn-getstarted" href="/autentificare">Autentificare</a>
        <% } %>

    </div>
</header>
